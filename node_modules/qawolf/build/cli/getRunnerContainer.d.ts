import Docker, { Container } from "dockerode";
declare type GetRunnerContainerResponse = {
    container: Container | null;
    isRunning: boolean;
};
export default function getRunnerContainer(docker: Docker): Promise<GetRunnerContainerResponse>;
export {};
