import Docker from "dockerode";
declare type DockerProgressDetail = {
    current: number;
    total: number;
};
declare type DockerProgressEvent = {
    id: string;
    progressDetail: DockerProgressDetail;
    status: string;
};
declare type ProgressFn = (event: DockerProgressEvent) => void;
declare type DoneFn = () => void;
export declare const prepareMultiProgressBar: () => [ProgressFn, DoneFn];
export declare const prepareSingleProgressBar: () => [ProgressFn, DoneFn];
export default function pullRunnerImage(docker: Docker, verbose?: boolean): Promise<void>;
export {};
