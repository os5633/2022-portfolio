declare type CreateSuite = {
    branch?: string;
    environment?: string;
    tags?: string;
    triggerId?: string;
    variables?: string;
};
declare type RunTests = CreateSuite & {
    wait: boolean;
};
export default function runTests({ branch, environment, tags, triggerId, variables, wait, }: RunTests): Promise<void>;
export {};
