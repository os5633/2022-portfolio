import { Frame, Page } from "playwright";
export interface GetValueOptions {
    timeout?: number;
}
export declare const getValue: (pageOrFrame: Page | Frame, selector: string, options?: GetValueOptions) => Promise<boolean | string>;
