import { Frame, Page } from "playwright";
export interface AssertElementOptions {
    timeout?: number;
}
export declare const assertElement: (pageOrFrame: Page | Frame, selector: string, options?: AssertElementOptions) => Promise<void>;
